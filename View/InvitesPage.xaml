<Page
    x:Class="WindowsFront_end.View.InvitesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsFront_end.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:model="using:WindowsFront_end.Models"
    mc:Ignorable="d"
   Background="LightBlue">
    <Page.Resources>
        <DataTemplate
            x:Key="InviteList"
            x:DataType="model:Person"
            >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="1"
                    Text="{x:Bind Name}">
                    
                </TextBlock>
            </Grid>
    
        </DataTemplate>
    </Page.Resources>
  

    <Grid>
        <ListView
            ItemsSource="{Binding Person.Trips}"
            ItemTemplate="{StaticResource InviteList}"
            />

    </Grid>
</Page>
